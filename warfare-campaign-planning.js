(function(root,factory){
 const api=factory(root&&root.WorldForgeWarfareSupply);
 if(typeof module!=='undefined'&&module.exports)module.exports=api;
 if(root)root.WorldForgeWarfareCampaignPlanning=api;
})(typeof window!=='undefined'?window:globalThis,function(Warfare){
'use strict';
if(!Warfare)throw new Error('WorldForge v5.0 Campaign Planning requires Warfare & Supply');
const VERSION='5.0.20',clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),round=(v,d=3)=>Number(Number(v).toFixed(d));
const REVIEW_COOLDOWN_YEARS=2,FAILED_OFFENSIVE_LIMIT=2;
function state(S){S.campaignPlanning=S.campaignPlanning||{version:VERSION,lastAppliedYear:null,plans:[],history:[],events:[],stats:{campaigns:0,primary:0,secondary:0,offensive:0,defensive:0,recovering:0,reassessed:0,failedOffensives:0}};const P=S.campaignPlanning;P.version=VERSION;P.plans=P.plans||[];P.history=P.history||[];P.events=P.events||[];P.stats=P.stats||{};return P}
function campaignKey(c){return String(c?.campaignId||c?.id||`campaign:${c?.warId}:${c?.frontId||'primary'}`)}
function frontFor(A,id){return(A.fronts||[]).find(f=>String(f.id)===String(id))||null}
function frontArmies(S,front){if(!front)return[];const ids=new Set([...(front.attackerArmyIds||[]),...(front.defenderArmyIds||[])].map(String));return(S.armies||[]).filter(a=>ids.has(String(a.id)))}
function supplyFactor(S,front){const rows=frontArmies(S,front);if(!rows.length)return .5;return clamp(rows.reduce((n,a)=>n+Number(a.supply??.5),0)/rows.length,0,1)}
function reinforcementFactor(S,front){const direct=Number(front?.reinforcementFulfillment);if(Number.isFinite(direct))return clamp(direct,0,1);const req=(S.strategicReserves?.requests||[]).find(r=>String(r.frontId)===String(front?.id));return clamp(Number(req?.fulfillment||0),0,1)}
function momentum(prev,front,S){const bal=Number(front?.balance||0),reinforce=reinforcementFactor(S,front),collapse=Number(front?.collapseRisk||0),supply=supplyFactor(S,front),last=Number(prev?.momentum||0);return round(clamp(last*.42+bal*.82+(reinforce-.5)*.22+(supply-.5)*.24-collapse*.38,-1,1))}
function posture(m,collapse,supply){if(collapse>.62||m<-.3||supply<.28)return'defensive';if(m>.3&&supply>.48)return'offensive';return'balanced'}
function phaseFor(prev,m,collapse,supply,failed,role){if(collapse>.7||supply<.22)return'recover';if(failed>=FAILED_OFFENSIVE_LIMIT&&m<.12)return'hold';if(role==='secondary'&&m<.28)return m<-.28?'defensive':'hold';if(m>.5)return'exploit';if(m>.18)return'offensive';if(m<-.38)return'defensive';if(prev?.phase==='prepare'&&Number(prev.yearsActive||0)<2)return'prepare';return'hold'}
function progress(prev,campaign,front,m){const targetChanged=prev&&String(prev.targetCityId??'')!==String(campaign?.targetCityId??'');let p=targetChanged?.08:Number(prev?.objectiveProgress||0);p+=m>.35?.16:m>.08?.07:m<-.35?-.12:m<-.1?-.05:.01;if(front?.status==='attacker-advantage')p+=.05;if(front?.status==='defender-advantage')p-=.04;if(campaign?.theaterRole==='secondary')p*=.96;return round(clamp(p,0,1))}
function reviewDecision(campaign,prev,ctx){let objective=prev?.objectiveType||campaign?.objectiveType||'hold-line',reason='continue-plan',emergency=false;if(!prev)return{objective:campaign?.objectiveType||'hold-line',reason:'campaign-opened',emergency:false};if(String(prev.targetCityId??'')!==String(campaign?.targetCityId??'')){objective=campaign?.objectiveType||objective;reason='target-changed';emergency=true}else if(ctx.collapse>.68){objective='hold-line';reason='front-collapse-risk';emergency=true}else if(ctx.supply<.25){objective='hold-line';reason='supply-failure';emergency=true}else if(ctx.failed>=FAILED_OFFENSIVE_LIMIT&&ctx.m<.12){objective='hold-line';reason='repeated-offensive-failure'}else if(ctx.m<-.5&&objective!=='hold-line'){objective='hold-line';reason='momentum-collapse';emergency=true}else if(ctx.m>.48&&objective==='hold-line'&&ctx.supply>.5){objective=campaign?.goal==='conquest'?'capture-capital':campaign?.goal==='punitive'?'break-army':'seize-border';reason='momentum-opportunity'}return{objective,reason,emergency}}
function mayReview(prev,year,decision){if(!prev)return true;if(decision.emergency)return true;return year-Number(prev.lastReassessmentYear??prev.startedYear??year)>=REVIEW_COOLDOWN_YEARS}
function event(P,year,plan,reason){const id=`campaign-event:${campaignKey(plan)}:${year}:${reason}`;if(!P.events.some(e=>e.id===id))P.events.push({id,year,campaignId:plan.campaignId,warId:plan.warId,frontId:plan.frontId,theaterRole:plan.theaterRole,reason,objectiveType:plan.objectiveType,phase:plan.phase,momentum:plan.momentum});P.events=P.events.slice(-240)}
function apply(w,force=false){const S=w.warfareSupply||Warfare.initialize(w);if(!S)return S;const A=S.strategicAI||{},P=state(S),year=Number(S.currentYear??w.history?.currentYear??0);if(P.lastAppliedYear===year)return S;const previous=new Map(P.plans.map(x=>[campaignKey(x),x])),plans=[];let reassessed=0;for(const campaign of A.campaigns||[]){const key=campaignKey(campaign),prev=previous.get(key),front=frontFor(A,campaign.frontId),m=momentum(prev,front,S),supply=supplyFactor(S,front),reinforce=reinforcementFactor(S,front),collapse=Number(front?.collapseRisk||0),role=campaign.theaterRole||prev?.theaterRole||'primary';const failed=Number(prev?.failedOffensives||0)+((prev&&['offensive','exploit'].includes(prev.phase)&&m<-.12)?1:0),decision=reviewDecision(campaign,prev,{m,supply,reinforce,collapse,failed}),review=mayReview(prev,year,decision),objective=review?decision.objective:(prev?.objectiveType||campaign.objectiveType||'hold-line');const yearsActive=prev?Number(prev.yearsActive||1)+1:1,phase=phaseFor(prev,m,collapse,supply,failed,role),phaseEnteredYear=prev?.phase===phase?Number(prev.phaseEnteredYear??year):year;const changed=!!prev&&review&&(objective!==prev.objectiveType||decision.reason!=='continue-plan');if(changed)reassessed++;const rolePriority=role==='primary'?.08:-.06;const plan={id:`plan:${key}`,campaignId:campaign.id||key,warId:campaign.warId,frontId:campaign.frontId,theaterRole:role,goal:campaign.goal,objectiveType:objective,targetCityId:campaign.targetCityId??prev?.targetCityId??null,posture:posture(m,collapse,supply),phase,momentum:m,previousMomentum:Number(prev?.momentum||0),objectiveProgress:progress(prev,campaign,front,m),priority:round(clamp(Number(campaign.priority||.5)+rolePriority+(m<0?Math.abs(m)*.1:m*.08)+collapse*.08,0,1)),supplyReliability:round(supply),reinforcementFulfillment:round(reinforce),collapseRisk:round(collapse),failedOffensives:failed,yearsActive,startedYear:Number(prev?.startedYear??year),phaseEnteredYear,lastReassessmentYear:changed||!prev?year:Number(prev?.lastReassessmentYear??year),nextReviewYear:year+REVIEW_COOLDOWN_YEARS,reassessmentReason:review?decision.reason:'cooldown-hold',status:'active'};if(changed||!prev)event(P,year,plan,plan.reassessmentReason);plans.push(plan)}for(const [key,old] of previous)if(!plans.some(x=>campaignKey(x)===key)){const sibling=plans.some(x=>String(x.warId)===String(old.warId)),reason=sibling?'front-closed':'war-ended',closed={...old,status:'closed',closedYear:year,reassessmentReason:reason};if(!P.history.some(x=>String(x.id)===String(closed.id)&&Number(x.closedYear)===year))P.history.push(closed);event(P,year,closed,reason)}P.plans=plans;P.history=P.history.slice(-180);P.lastAppliedYear=year;P.stats={campaigns:plans.length,primary:plans.filter(x=>x.theaterRole==='primary').length,secondary:plans.filter(x=>x.theaterRole==='secondary').length,offensive:plans.filter(x=>['offensive','exploit'].includes(x.phase)).length,defensive:plans.filter(x=>['defensive','hold'].includes(x.phase)).length,recovering:plans.filter(x=>x.phase==='recover').length,reassessed,failedOffensives:plans.reduce((n,x)=>n+Number(x.failedOffensives||0),0),advancing:plans.filter(x=>x.momentum>.18).length,stalled:plans.filter(x=>Math.abs(x.momentum)<=.1).length,retreating:plans.filter(x=>x.momentum<-.38).length};S.stats=S.stats||{};Object.assign(S.stats,{campaignPlans:P.stats.campaigns,primaryCampaignPlans:P.stats.primary,secondaryCampaignPlans:P.stats.secondary,offensiveCampaigns:P.stats.offensive,defensiveCampaigns:P.stats.defensive,recoveringCampaigns:P.stats.recovering,reassessedCampaigns:P.stats.reassessed,failedCampaignOffensives:P.stats.failedOffensives,advancingCampaigns:P.stats.advancing,stalledCampaigns:P.stats.stalled,retreatingCampaigns:P.stats.retreating});return S}
const baseSummary=Warfare.summary?.bind(Warfare);if(baseSummary)Warfare.summary=function(w){const s=baseSummary(w),P=w.warfareSupply?.campaignPlanning?.stats||{};return{...s,campaignPlans:Number(P.campaigns||0),primaryCampaignPlans:Number(P.primary||0),secondaryCampaignPlans:Number(P.secondary||0),offensiveCampaigns:Number(P.offensive||0),defensiveCampaigns:Number(P.defensive||0),recoveringCampaigns:Number(P.recovering||0),reassessedCampaigns:Number(P.reassessed||0),failedCampaignOffensives:Number(P.failedOffensives||0),advancingCampaigns:Number(P.advancing||0),stalledCampaigns:Number(P.stalled||0),retreatingCampaigns:Number(P.retreating||0)}};
Warfare.applyCampaignPlanning=apply;Warfare.CAMPAIGN_PLANNING_VERSION=VERSION;
return{VERSION,REVIEW_COOLDOWN_YEARS,FAILED_OFFENSIVE_LIMIT,campaignKey,frontFor,supplyFactor,reinforcementFactor,momentum,posture,phaseFor,progress,reviewDecision,mayReview,apply};
});